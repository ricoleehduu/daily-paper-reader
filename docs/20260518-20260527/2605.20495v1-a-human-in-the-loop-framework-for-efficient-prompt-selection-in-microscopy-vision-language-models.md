---
title: A Human-in-the-Loop Framework for Efficient Prompt Selection in Microscopy Vision-Language Models
title_zh: 人机协同框架下的显微视觉语言模型高效提示选择
authors: "Abhiram Kandiyana, Ankur Mali, Lawrence O. Hall, Peter R. Mouton, Dmitry Goldgof"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20495v1"
tags: ["query:mit"]
score: 9.0
evidence: 人机协同提示选择用于显微镜视觉语言模型分类，减少标注工作量。
tldr: 针对显微镜图像分类中视觉语言模型（VLM）提示调优仍需专家验证和编辑的问题，提出了一种人机协同的提示选择框架。该框架通过主动学习优先选择未标注图像，让专家验证并轻量编辑VLM草案字幕，从而高效构建小样本提示集。实验表明，该方法在大幅减少人工标注工作量的同时，保持了较高的分类准确率，为显微镜图像分析中VLM的低成本部署提供了可行方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.20495v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20495v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 876, \"height\": 298, \"label\": \"Table\"}]"
motivation: 人机协同提示选择用于显微镜视觉语言模型分类，减少标注工作量。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
用于显微图像分类的深度学习流程通常需要昂贵、劳动密集且耗时长的专家标注，以生成高质量的训练真值。近期研究表明，通过提示调优视觉语言模型（VLMs），可以构建一个由专家验证的图像-描述对组成的小型提示集，作为少样本上下文在推理时对所有剩余图像进行分类，从而减少人工标注。为进一步降低工作量，VLM可以为候选示例起草描述，然后由专家验证并稍作编辑，而非从头撰写文本。然而，有两个实际问题仍未解决：（1）应优先选择哪些未标注图像进行验证；（2）需要多少个已验证示例才能达到性能目标。在本工作中，我们将提示集构建形式化为目标驱动的主动学习问题，以确定优先标注哪些图像，从而解决这些问题。我们在严格低资源约束且未标注样本池较小的情况下，研究了三种互补的选择标准。实验表明，与随机选择相比，我们的方法以少得多的专家验证图像达到目标性能，平均仅需20张标注图像即可实现100%的测试准确率。更广泛地说，我们的人机协同框架展示了在生物医学图像分析中以人为中心的生成式人工智能应用方式，专家仍积极参与验证和优化模型输出，同时显著降低标注成本。代码和数据将公开发布。

## Abstract
Deep-learning pipelines for microscopy image classification often require expensive, labor- and time-intensive expert annotation to produce high-quality ground truth for training. Recent work has shown that prompt tuning of vision-language models (VLMs) can reduce manual annotation by constructing a small prompt set of expert-verified image-caption exemplars that is reused as few-shot context to classify all remaining images at inference time. To further reduce effort, the VLM can draft captions for candidate exemplars, which experts then verify and lightly edit instead of writing text de novo. However, two practical questions remain unaddressed: (1) which unlabeled images should be prioritized for verification, and (2) how many verified exemplars are needed to reach a performance target. In this work, we address these questions by formulating prompt-set construction as a target-driven active learning problem that prioritizes which images to annotate. We study three complementary selection criteria under strict low-resource constraints with small unlabeled pools. Experiments show that our methods reach the target performance with substantially fewer expert-verified images than random selection, achieving 100% test accuracy with as few as 20 annotated images on average. More broadly, our human-in-the-loop framework demonstrates a human-centered use of generative AI in biomedical image analysis, where experts remain actively involved in verifying and refining model output while significantly reducing annotation cost. Code and data will be publicly available.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

*   **问题背景**：显微图像（如脑组织切片）的细胞损伤评估对神经疾病研究至关重要，但传统人工计数或深度学习方法都需要大量专家标注，成本高、周期长。
*   **现有方案局限**：视觉语言模型（VLM）可通过提示调优（如 APT‑USF）利用少量专家验证的图像‑描述对作为少样本上下文来分类，但仍需专家逐一验证或轻量编辑描述；关键难题在于：
    *   **选什么图片**：哪些未标注样本最值得优先标注？
    *   **多少张图片**：需要积累多少验证示例才能达到预设性能？
*   **整体含义**：将提示集的迭代构建转化为**目标驱动的主动学习（Active Learning）问题**，以最少的人工标注代价实现预定的验证性能目标，从而显著降低显微图像分析中的人机协同成本。

## 2. 论文提出的方法论

*   **总体框架**： **目标驱动的主动提示调优（目标‑driven AL‑based prompt tuning）**  
    *   从很小的种子提示集开始（10 张图像）。
    *   每轮选择 `m=10` 张未标注图像，让 VLM 在现有提示集下生成候选描述，专家验证/修正后扩充提示集，重复直到验证集平均类别准确率达到 80% 或其他停止条件（最大轮次 `Rmax=7`，或预算耗尽）。
    *   三个**采集函数**（下一轮选图策略）是核心区别。

*   **关键技术细节**：
    1.  **不确定性引导采集 (APT‑U)**  
        *   对每张未标注图像，利用随机解码（温度采样）生成 `T=5` 个输出，构建经验类别分布 `p_y`（公式 2）。
        *   计算熵 `u_ent`（公式 3）作为不确定性得分，选 `Top‑m` 最高者（公式 4）。
    2.  **复杂度感知不确定性采集 (APT‑CA)**  
        *   同时考虑预测不确定性 `u`（1 − 最大类别概率，公式 6）和描述复杂度 `κ`（公式 8）。
        *   复杂度由两惩罚项构成：**长度惩罚** `ℓ`（归一化 token 长度）和**冗余惩罚** `ρ`（与已有描述的平均余弦相似度）。
        *   使用拉格朗日松弛将约束优化问题转化为得分 `s = u – λ_c * κ`，选最高得分（公式 9‑10）。超参数 `α=0.01, β=0.1, λ_c=0.5` 由验证集调整。
    3.  **密度树边界采集 (APT‑DTB)**  
        *   在图像编码器（如 BioMedCLIP）嵌入空间中，基于 k‑NN 图构建**密度树**（steepest‑uphill tree）：先计算局部密度 `ρ_i`（公式 11），再通过寻找高密度近邻建立父母关系（公式 13‑14），形成密度聚类。
        *   定义**边界分数** `b(i)`：某点的邻居中属于不同簇的比例（公式 15），优先选取位于簇间山谷区域的样本，同时通过限制小簇选择和簇内单一采样来避免采集集体异常点。
        *   每轮取 `Top‑m` 边界分数最高的样本。

*   **标注与推理**：  
    *   每轮选出 `A^{(r)}` 后，VLM 生成候选描述，专家修正形成 `\widetilde{A}^{(r)}`，加入提示集 `P^{(r)}`。
    *   达到停止条件后，用最终提示集对测试集做少样本推理。

## 3. 实验设计

*   **数据集**：Lurcher 显微图像数据集，21 只小鼠（11 Lurcher 突变型，10 野生型），10× 放大、Cresyl violet 染色，任务是区分突变与正常。
*   **评估协议**：四折留一验证，每折测试集 2 只（每类 1 只），验证集 2 只，其余 17 只作为训练池，初始种子提示集来自 2 只。
*   **对比方法**：
    *   基线：**APT‑USF**（随机选择未标注图像，提示集固定扩展至 36 张）。
    *   提出的三种变体：APT‑U、APT‑CA、APT‑DTB。
*   **评估指标**：
    *   图像级平均类别准确率（%）。
    *   动物级准确率（多数投票，%）。
    *   达到停止条件所需标注图像数量（越低越好）。
*   **实施细节**：VLM 固定为 **GPT‑4o**（通过 OpenAI API），每轮从未标注池随机采样 100 张图像作为候选池进行采集，确保低资源场景。

## 4. 资源与算力

*   **文中未明确说明**本地 GPU 型号、数量或训练时长。方法完全基于调用 OpenAI 的 GPT‑4o API，无模型微调或本地大规模训练，因此算力消耗主要体现在 API 调用费用和网络延迟，而非传统 GPU 算力。作者未报告 API 调用总量或成本。

## 5. 实验数量与充分性

*   **主要实验**：在单一数据集（Lurcher）上进行四折交叉验证，比较 3 种采集方法与随机基线，共 4 组方法 × 4 折 ≈ 16 次独立运行。
*   **消融实验**：对 APT‑DTB 进行了**嵌入骨干网络的消融**，比较了 CLIP、BioMedCLIP、MedSigLIP 三种嵌入模型（3 折），分析不同预训练表示对采集效率的影响。
*   **充分性与公平性**：
    *   实验设计在同一数据集上进行了多折验证，且与相同的 VLM（GPT‑4o）和相同基线公平比较。
    *   超参数（如 `m`, 候选池大小 100, `T=5`, 停止阈值 80%）经过验证集或领域专家确定，具有一致性。
    *   然而，**数据集单一**限制了结论的普遍性；缺少与其他主动学习策略（如基于深度特征的不确定性/多样性混合方法）或不同 VLM 基模型的对比；也未分析不同停止阈值、每轮示例数 m 的影响。

## 6. 论文的主要结论与发现

*   **效率大幅提升**：所提采集函数平均仅需 **20~25 张** 专家验证图像即可达到 80% 验证准确率目标，远低于基线 APT‑USF 的 36 张，且均达到 **100%** 动物级测试准确率。
*   **性能一致性**：图像级平均类别准确率也均优于随机基线（APT‑U 87.8%，APT‑CA 88.6%，APT‑DTB 86.5% vs. APT 80.1%）。
*   **嵌入骨干影响**：在密度树边界采集中，**BioMedCLIP** 比通用 CLIP 或更大规模的 MedSigLIP 效率更高（27 张 vs. 40 张），说明领域相关的预训练对结构感知采集更有利。
*   **框架价值**：证明以主动学习方式动态构建提示集，能在极小标注预算下实现高性能显微图像分类，使专家从繁重标注中解放，仅进行轻量级验证‑编辑。

## 7. 优点

*   **创新的问题形式化**：将提示集构建明确定义为目标驱动的主动学习，兼顾效率与性能阈值。
*   **互补的采集策略**：提供了**不确定性、复杂度感知、几何结构**三种不同视角的采集函数，可灵活适应不同数据特性。
*   **人机协同闭环**：专家只做轻量验证和编辑，而非从头撰写描述，切实降低人工负担。
*   **完整的实验验证**：在真实的显微数据集上，同时报告标注效率、图像级和动物级准确率，结果一致且显著。
*   **开源承诺**：代码和数据将公开，有助于复现和后续研究。

## 8. 不足与局限

*   **数据集单一且规模小**：仅在一种显微图像数据集（Lurcher）上验证，难以证明方法在其他染色、器官、物种或模态上的泛化能力。
*   **VLM 依赖性强**：所有实验基于 GPT‑4o（闭源商业 API），未测试其他开源或多模态模型（如 LLaVA、Fuyu）对采集策略的影响；API 成本和时间可能是实践瓶颈。
*   **消融范围有限**：
    *   只对 APT‑DTB 的嵌入骨干进行了消融，未探索每轮 m、候选池大小、停止目标、复杂度权重等关键超参数的敏感性。
    *   未与更先进的主动学习基线（如多样性采样、贝叶斯主动学习等）进行对比。
*   **密度树方法的局限性**：APT‑DTB 需预先计算嵌入，采集质量高度依赖嵌入模型；对簇内只选一个可能忽略类内多样性的边界样本。
*   **停止准则的设定**：验证目标 80% 由专家商定，对更易或更难的分类任务可能需要重新设定，且基于验证集可能引入过拟合风险。
*   **未讨论 VLM 的随机性**：GPT‑4o 的输出具有随机性，尽管使用了 T=5 采样，但重复运行的波动性未给出置信区间。

（完）
