---
title: A Human-in-the-Loop Framework for Efficient Prompt Selection in Microscopy Vision-Language Models
title_zh: 一种用于显微镜视觉-语言模型中高效提示选择的人机协同框架
authors: "Abhiram Kandiyana, Ankur Mali, Lawrence O. Hall, Peter R. Mouton, Dmitry Goldgof"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20495v1"
tags: ["query:mit"]
score: 9.0
evidence: 通过人机协同为显微视觉语言模型高效选择提示，直接推进医学图像理解VLM
tldr: "显微图像分类依赖昂贵的人工标注，视觉语言模型提示调优通过构建少量专家验证的示例集来降低标注，但示例选择仍低效。本文将提示集构建形式化为目标驱动的主动学习问题，提出三种互补选择标准，在低资源约束下优先标注关键图像。实验显示，平均仅需20张专家验证图像即达100%测试准确率，大幅超越随机选择。该框架展示了人机协同生成式AI在生物医学图像分析中的应用范例。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.20495v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20495v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 876, \"height\": 298, \"label\": \"Table\"}]"
motivation: 降低显微图像分类的专家标注成本，并解决提示调优中关键图像选择与所需数量确定的问题。
method: 将提示集构建建模为目标驱动的主动学习，研究三种互补选择标准，在低资源下优先标注最具信息量的图像。
result: "平均仅需20张专家验证图像即实现100%测试准确率，显著少于随机选择方法。"
conclusion: 提出的人机协同框架高效减少标注，验证了生成式AI在生物医学图像分析中的人类中心应用价值。
---

## 摘要
用于显微镜图像分类的深度学习流程通常需要昂贵、耗费人力且耗时的专家标注，以生成高质量的训练真值。近期研究表明，视觉-语言模型（VLMs）的提示微调可以通过构建一个由专家验证的图像-标题范例组成的小型提示集来减少手动标注，该提示集在推理时作为少样本上下文重复用于分类所有剩余图像。为了进一步减少工作量，VLM可以为候选范例草拟标题，然后专家进行验证并轻松编辑，而无需从头编写文本。然而，仍有两个实际问题未解决：（1）应优先选择哪些未标记图像进行验证，（2）需要多少已验证的范例才能达到性能目标。在本文中，我们将提示集构建表述为一个目标驱动的主动学习问题，以优先选择要标注的图像，从而解决这些问题。我们在严格低资源约束条件下，使用小型未标记池研究了三种互补的选择标准。实验表明，我们的方法达到目标性能所需的专家验证图像数量明显少于随机选择，平均仅需20张标注图像即可实现100%的测试准确率。更广泛地说，我们的人机协同框架展示了在生物医学图像分析中以人为中心的生成式人工智能应用，专家在其中积极参与验证和完善模型输出，同时显著降低标注成本。代码和数据将公开发布。

## Abstract
Deep-learning pipelines for microscopy image classification often require expensive, labor- and time-intensive expert annotation to produce high-quality ground truth for training. Recent work has shown that prompt tuning of vision-language models (VLMs) can reduce manual annotation by constructing a small prompt set of expert-verified image-caption exemplars that is reused as few-shot context to classify all remaining images at inference time. To further reduce effort, the VLM can draft captions for candidate exemplars, which experts then verify and lightly edit instead of writing text de novo. However, two practical questions remain unaddressed: (1) which unlabeled images should be prioritized for verification, and (2) how many verified exemplars are needed to reach a performance target. In this work, we address these questions by formulating prompt-set construction as a target-driven active learning problem that prioritizes which images to annotate. We study three complementary selection criteria under strict low-resource constraints with small unlabeled pools. Experiments show that our methods reach the target performance with substantially fewer expert-verified images than random selection, achieving 100% test accuracy with as few as 20 annotated images on average. More broadly, our human-in-the-loop framework demonstrates a human-centered use of generative AI in biomedical image analysis, where experts remain actively involved in verifying and refining model output while significantly reducing annotation cost. Code and data will be publicly available.

---

## 论文详细总结（自动生成）

# 论文总结：显微镜视觉-语言模型中高效提示选择的人机协同框架

## 1. 论文的核心问题与整体含义
- **研究背景**：显微镜图像分类（如神经退行性疾病研究中的细胞损伤评估）依赖专家进行耗时费力的手动标注。传统深度学习方法需要大量标注数据和重复微调，难以适应染色、成像条件等变化。
- **核心问题**：视觉-语言模型（VLM）的提示微调虽然能通过少量范例减轻标注负担，但**哪些未标记图像应优先标注，以及需要多少验证范例才能达到性能目标**这两个关键问题尚未解决。盲目随机选择会导致冗余或低效标注。
- **整体含义**：本文将提示集构建形式化为**目标驱动的主动学习问题**，提出一种**人机协同框架**，使专家只需验证并轻量编辑少量关键图像的标题，即可快速达到预设性能，从而显著降低显微镜图像分析中的专家标注成本。

## 2. 论文提出的方法论
- **整体流程**：目标驱动的主动提示微调。从一个很小的种子提示集（10张图像）开始，迭代执行：
  1. 从无标签池中根据**采集函数**选择一个小规模“活跃集”（每轮10张）。
  2. 由冻结的VLM（GPT-4o）为活跃集图像生成候选标题（包含标签和推理过程）。
  3. 专家验证并修正标题，将修正后的图像-标题对追加到提示集中。
  4. 在验证集上评估，一旦达到目标平均类别准确率（80%）或达到轮次上限，即终止循环。
- **三种互补的采集函数（核心创新）**：
  - **不确定性引导采集（APT-U）**：通过随机解码生成多个输出，构建类别经验分布，计算**熵**作为不确定性得分，选择得分最高的图像。公式：\(u_{\text{ent}}(x) = -\sum_y p_y \log p_y\)。
  - **复杂度感知不确定采集（APT-CA）**：在不确定性基础上，引入**标题复杂度**约束（长度和与已有提示集的语义冗余），通过拉格朗日松弛将约束优化转化为：得分 = 不确定性 - λ × 复杂度。选择得分最高的样本，以鼓励简洁、非冗余的范例。
  - **密度树边界采集（APT-DTB）**：在VLM图像编码器的嵌入空间（默认BioMedCLIP）中构建k近邻图，使用**局部密度**和**最陡上坡树**划分密度簇，计算**边界得分**（邻域中属于不同簇的比例），优先选取靠近簇边界的样本，同时抑制微小孤立簇（集体异常），以定位决策模糊区域。
- **停止条件**：验证集平均类别准确率达到80%，或累计标注超出预算，或达到最大轮次（7轮）。每轮从无标签池中随机抽取100张作为候选子集进行选择，模拟低资源场景。

## 3. 实验设计
- **数据集**：Lurcher显微图像数据集，21只小鼠（11只Lurcher突变，10只野生型）。任务为二分类。低倍镜10×，Cresyl violet染色。图像级别的标签最终通过多数投票汇聚为动物级别诊断。
- **评估协议**：四折留一验证。每折取2只动物（每类一只）为测试集，2只动物为验证集，剩下17只动物的图像用于训练（初始10张种子则来自其中2只动物，其余为无标签池）。
- **基线方法**：APT-USF（随机选择活跃集，扩充到固定36张图像）。
- **对比方法**：三种提出的采集函数变体——APT-U、APT-CA、APT-DTB。
- **消融实验**：针对APT-DTB，评估三种不同的图像编码器骨干（通用CLIP、生物医学BioMedCLIP、医学SigLIP模型MedSigLIP）对标注效率的影响。

## 4. 资源与算力
- 论文未明确提及其使用的具体GPU型号、数量或训练时长。所有主要实验均通过调用OpenAI API的GPT-4o完成，因此算力消耗主要在API调用端，而非本地大规模训练。文本提及使用轻量级Sentence-Transformer编码器进行标题嵌入，计算量较小，未给出硬件细节。

## 5. 实验数量与充分性
- **主实验组数**：在Lurcher数据集上进行了4折实验，比较了3种提出方法和1个基线（共4组方法 × 4折 = 16次独立运行），分别报告了标注图像数量、图像级准确率和动物级准确率。
- **消融实验**：针对APT-DTB，在3种嵌入骨干下进行了3折实验（9次运行）。
- **充分性评价**：
  - 实验规模适中，但仅在**单一生物医学数据集**上进行评估，缺少多数据集的泛化验证。
  - 基线仅与随机选择对比，未与领域内更先进的主动学习策略（如CEC等）直接比较。
  - 保持了一致的VLM（GPT-4o）、固定的超参数和评估协议，内部比较较为公平。超参数基于小型验证扫描选定，但未系统报告敏感度。
  - 整体上，实验能支撑论文核心主张，展示了采集函数相对于随机选择的明显优势，但**外部泛化性和与其他主动学习范式的横向对比仍显不足**。

## 6. 论文的主要结论与发现
- 三种目标驱动的采集函数均**显著提升了标注效率**，在仅需平均约20张专家验证图像（远少于基线36张）的情况下即可达到100%动物级测试准确率。
- APT-U标注效率最高（20张），APT-CA图像级准确率最高（平均88.6%），APT-DTB在合适的领域嵌入（BioMedCLIP）下表现优秀。
- 通过人机协同和生成式AI辅助，能够大幅减少专家编写标题的工作量，同时保持模型输出的高质量。
- 嵌入空间的质量影响密度树边界采集的效果，领域专用的预训练模型（BioMedCLIP）优于通用或更大规模的医学模型。

## 7. 优点
- **问题定义清晰**：将提示集构建形式化为目标驱动的主动学习问题，回答了实际应用中的“选什么”和“选多少”两个关键问题。
- **方法多样性**：提供了三种互补的采集原则（不确定性、复杂度、嵌入结构），覆盖多种低资源约束场景。
- **低资源高效**：在严格限制候选池（每轮100张）和少量标注下实现100%准确率，非常贴近真实生物医学场景的预算约束。
- **人机协同实用**：利用VLM生成初始标题，专家仅需修正而非从头编写，降低了认知负担，且保持专家在决策环中的主动角色。
- **可解释性**：提示中包含链式思维理由，专家可验证推理过程，符合临床要求。

## 8. 不足与局限
- **数据集单一**：仅在一个显微镜二分类任务上验证，缺少对多类、多模态、更多染色/成像条件的通用性证据。
- **基线对比有限**：仅与随机选择比较，未对比其他基于不确定性或多样性的主动学习基线（如CEC、核心集方法等），难以说明方法的绝对领先性。
- **VLM依赖单一**：所有实验均基于GPT-4o，未探讨不同VLM（如开源模型或不同规模模型）下的采集行为差异。
- **超参数敏感度未充分分析**：多个超参数（候选池大小、每轮样本数、复杂度权重等）通过小型验证选定，但未报告不同设置下的性能变化，实际应用调参成本可能较高。
- **密度树采集的计算约束**：需要预训练的图像编码器，且假设嵌入空间能有效区分决策边界，在特征质量不佳的情况下可能失效；微小簇处理规则虽抑制了异常，但可能丢失某些有效信息。
- **动物级别评估的局限性**：动物级准确率已达100%，导致该指标失去了对不同方法的区分度，未来需要更具挑战性的数据集。
- **未讨论成本细节**：虽然标注图像数量是效率指标，但API调用次数、延迟和专家修正时间等实际成本并未量化。

（完）
