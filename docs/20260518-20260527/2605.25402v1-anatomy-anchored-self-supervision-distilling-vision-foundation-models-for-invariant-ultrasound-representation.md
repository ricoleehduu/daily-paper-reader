---
title: "Anatomy-Anchored Self-Supervision: Distilling Vision Foundation Models for Invariant Ultrasound Representation"
title_zh: 解剖锚定的自监督：蒸馏视觉基础模型以学习不变超声表示
authors: "Chunzheng Zhu, Yijun Wang, Jianxin Lin, Feng Wang, Hongwei Wang, Lei Zhao, Shengli Li, Kenli Li"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25402v1"
tags: ["query:mit"]
score: 9.0
evidence: 利用LP-SAM模块，通过可学习提示实现无标注的超声解剖结构勾画，蒸馏基础模型。
tldr: 超声图像自监督学习常忽略内部解剖结构，仅关注全局图像特征，难以学习临床语义对齐的表示。本文提出解剖锚定的自监督框架ANAUS，通过可学习潜在提示引擎和一次性域适应，赋能LP-SAM模块实现大规模无标注解剖分割，并设计双重自监督策略：视图间语义感知的解剖分离对齐与上下文核心区域预测。前者强制相同解剖区域的特征不变性并区分不同结构，后者通过重建受损区域捕获细节。在六个涵盖不同解剖部位的公开数据集上，ANAUS一致超越现有方法，且计算效率适应临床部署需求。该工作开创了临床驱动的超声自监督新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25402v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 664, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25402v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25402v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25402v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 767, \"height\": 172, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25402v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1254, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25402v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 549, \"height\": 439, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25402v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 571, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25402v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 598, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25402v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 621, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25402v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1074, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25402v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 678, \"height\": 478, \"label\": \"Table\"}]"
motivation: 现有超声自监督方法忽视解剖上下文，限制临床对齐的表示学习。
method: 提出ANAUS框架，利用潜在提示和域适应实现无标注解剖分割，并采用解剖分离对齐与核心区域预测双策略学习不变表示。
result: 在六个公开数据集上全面超越当前最先进方法，同时保持计算高效。
conclusion: 为医学超声提供了解剖锚定的自监督学习范式，推动临床高效部署。
---

## 摘要
自监督预训练范式在医学成像中学习可迁移表征方面日益受到重视，然而现有的超声图像方法在图像或帧级别操作，忽略了用于临床对齐表征学习的解剖上下文。在本工作中，我们提出了一种解剖锚定的超声自监督框架 ANAUS，将表征学习从通用视觉区域转向具有临床意义的解剖结构。利用可学习的潜在提示引擎以及现有公共图像-掩码对的一次性域适应，我们使 LP-SAM 模块能够实现大规模的无注释解剖描绘。基于这种解剖定位，我们提出了一种双策略自监督学习范式，包括跨视图语义感知的解剖分离对齐和上下文核心区域预测，以增强表征学习。具体来说，前者强制相同解剖区域内的特征不变性，同时促进不同结构间的可区分性；后者迫使模型重建被破坏的区域，从而捕获细粒度的结构细节。在六个公开数据集上的广泛评估表明，我们的方法（ANAUS）始终超越当前最先进方法，同时保持了临床部署所必需的计算效率。代码见 https://github.com/zhcz328/ANAUS。

## Abstract
Self-supervised pre-training paradigm has gained increasing prominence for learning transferable representations in medical imaging, yet existing methods for ultrasound (US) images operate at the image or frame level, overlooking the anatomical context for clinical-aligned representation learning. In this work, we propose an anatomy-anchored ultrasound self-supervision framework ANAUS that shifts representation learning from generic visual regions to clinically meaningful anatomical structures. Utilizing a learnable latent prompt engine alongside a one-time domain adaptation on existing public image--mask pairs, we empower the LP-SAM module to achieve annotation-free anatomy delineation at scale. Building upon this anatomical grounding, we propose a dual-policy self-supervised learning paradigm consisting of inter-view semantics-aware anatomy-separating alignment and contextual core-region prediction to enhance representation learning. Specifically, the former enforces feature invariance within identical anatomical regions while promoting discriminability across distinct structures; the latter compels the model to reconstruct corrupted regions, thereby capturing fine-grained structural details. Extensive evaluations on six public datasets demonstrate that \ours{} consistently outstrips current state-of-the-art methods while maintaining the computational efficiency essential for clinical deployment. Code is available at https://github.com/zhcz328/ANAUS.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景**：超声（US）成像因其便携、实时、无电离辐射等特性，是临床诊断的重要工具。深度神经网络在自动化超声分析中展现出巨大潜力，但其数据驱动的训练范式常因缺乏高质量标注而受限。自监督学习（SSL）通过从数据本身构建监督信号，成为解决该瓶颈的有效途径。
*   **核心问题与动机**：当前的超声自监督方法主要分为两类：
    *   **帧级方法**：利用视频帧间的时间一致性构建正样本对，但将整帧图像作为一个整体，可能将背景伪影和斑点噪声编码为有意义的特征，忽略了真正具有诊断价值的解剖结构。
    *   **区域级方法**：借鉴自然图像处理，采用通用分割（如K-means聚类）生成对比学习的区域。然而，由于超声图像中解剖结构边界对比度低、噪声干扰严重，这些通用方法难以准确隔离出有临床意义的解剖区域。
    *   **本质缺陷**：这两种范式均未能与临床中“以解剖结构为中心”的诊断逻辑对齐。临床诊断的核心是特定解剖标志的形态、纹理及空间关系，而非随机的图像块或整帧图像。
*   **整体含义**：本文提出从根本上将自监督学习的对比单元从通用视觉区域转向具有临床意义的解剖结构，以实现与临床认知相一致的超声表征学习。

### 2. 论文提出的方法论

论文提出了一个名为`AnaUS`的解剖锚定超声自监督学习框架，其核心思想是“先发现，再学习”：首先自动发现图像中的解剖结构（通过`LP-SAM`），然后在这些结构上进行高级表征学习。该方法论包含两个主要部分：

*   **技术组件 1：LP-SAM（潜在提示-分割一切模型）**
    *   **目标**：实现对未标注超声图像的自动、无人工提示的解剖结构分割。
    *   **核心思想**：解决基础模型`SAM`在自然图像与医学超声图像间的领域鸿沟，并使其具备无提示自动分割能力。
    *   **阶段 1：基于适配器的领域特化**
        *   冻结`SAM`图像编码器的预训练权重，在其每个ViT块中插入可学习的适配器（一种瓶颈结构的残差块，`d' << d`）。
        *   使用已有的公开超声图像-掩码对数据集，仅训练这些适配器，损失函数为二元交叉熵（`L_BCE`）和Dice损失（`L_Dice`）的组合。Dice损失对解决超声图像中小目标正负样本不平衡问题至关重要。
    *   **阶段 2：潜在提示引擎**
        *   在阶段 1 的基础上，冻结`SAM`及其适配器。引入一个可学习的查询Token `T` 和一个跨感知注意力（Cross-Perception Attention, CPA）模块。
        *   `CPA`模块首先将`SAM`编码器的全局ViT特征`F_v`和局部CNN特征`F_c`进行融合，以获取更丰富的表示。
        *   随后，`T`与融合特征通过多轮双向交叉注意力进行迭代优化，最终得到一个包含任务条件的潜在提示嵌入`T'{last}`。将此嵌入直接送入`SAM`的掩码解码器，即可自动生成解剖掩码。
        *   这两个阶段的训练是**一次性**的，完成之后`LP-SAM`被冻结，不再参与后续的自监督预训练。

*   **技术组件 2：双策略自监督学习范式**
    基于`LP-SAM`生成的解剖掩码，论文提出了两个协同学习目标来训练最终的编码器`f_θ`：
    1.  **视图不变解剖对比匹配**
        *   **视图构造**：对原图`x`施加两种数据增强`t`和`t'`，得到视图`v1`和`v2`，并将其对应的解剖掩码进行同步变换。
        *   **多尺度掩码池化**：将每个视图送入编码器得到多尺度特征图。对每个解剖掩码，在其对应区域进行均值池化（公式4），得到该解剖区域的表征向量`h_s`，从而消除背景干扰。
        *   **对比目标**：采用`BYOL`风格的非对称“在线-目标”网络架构。损失函数`L_ana`（公式5）强制来自`v1`和`v2`的同一个解剖结构的表征相互拉近，同时与其他解剖结构的表征推开，从而学习视图不变且解剖间可区分的表征。此操作在多个特征尺度上执行。

    2.  **解剖上下文预测**
        *   **核心区域提取**：提取两个增强视图`v1`和`v2`的重叠区域，确保重建目标是临床相关的核心内容，而非随机背景。
        *   **针对性破坏**：对重叠区域施加一个复合破坏函数`D`（公式6），包含三种模拟超声伪影的操作：`Shuff`（局部像素打乱模拟组织位移）、`N`（高斯噪声模拟斑点干扰）和`O`（随机遮挡模拟声影）。
        *   **重建目标**：在线编码器处理被破坏的图像块，目标编码器处理完整的图像块，两者产生的特征图通过均方误差损失`L_ctx`进行匹配。
    *   **最终预训练损失**：为上述两个损失的加权和（公式7）。

### 3. 实验设计

*   **数据集**：
    *   **`LP-SAM`微调**：使用`DDTI`、`TG3K`、`CAMUS`三个公开的超声图像-掩码数据集。
    *   **自监督预训练**：使用由`Butterfly`和`CAMUS`组成的联合数据集（称为`BF+CM`）。
    *   **下游评估**：
        *   **分类任务**：`POCUS`（肺部超声，COVID/普通肺炎/正常）和`BUSI`（乳腺超声，良/恶性/正常），评估准确率和F1分数。
        *   **分割任务**：`UDIAT-B`、`TN3K`（甲状腺结节）、`BUSI`和`HMC-QU`（心脏），评估PPV/精确率、灵敏度、Dice系数和豪斯多夫距离（HD）。
        *   所有下游任务均采用五折交叉验证。

*   **对比方法（Benchmark）**：
    *   **通用SSL**：监督学习、`ImageNet`预训练、`SimCLR`、`MoCo v2`。
    *   **目标中心SSL**：`SlotCon`（基于语义分组）、`DetCon`（基于多尺度组合分组）、`R2O`（基于K-means）。文中还测试了将这些方法中的通用分割替换为`LP-SAM`分割（以`†`标记）后的性能。
    *   **超声专用SSL**：`USCL`、`USF-MAE`、`USUCL`、`AWCL`、`IVPP`。这些方法进一步在`BF+CM`数据集上进行了预训练以实现公平对比（以`++`标记）。

### 4. 资源与算力

论文明确提到了训练所使用的资源与算力：
*   **硬件**：使用两块（2x）**NVIDIA RTX 4090 GPU**。
*   **时间和内存**：
    *   `LP-SAM`适配器微调：引入约13M参数，耗时约**24小时**。
    *   潜在提示引擎训练：增加约11.1M参数，耗时约**8小时**。
    *   完整的`AnaUS`自监督预训练（300 epochs）：耗时约**10小时**，占用约**22GB显存**。
    *   推理速度：最终保留的编码器处理一张224x224的图像仅需约**0.45毫秒**（在RTX 4090上），证实了其临床实时应用的潜力。

### 5. 实验数量与充分性

实验设计**非常充分和客观**，主要体现如下：

*   **对比实验广泛**：在6个公开数据集、两种下游任务（分类、分割）上，与超过**10种**基线方法（涵盖通用、目标中心和超声专用三大类）进行了系统对比。
*   **公平性考虑周全**：
    *   对于目标中心对比方法，提供了使用相同`LP-SAM`掩码的变体，排除了“分割质量”这一变量，直接对比学习框架本身。
    *   对于超声专用方法，确保它们在同样的更大规模数据集（`BF+CM`）上进行了预训练，消除了数据量带来的不公平优势。
*   **消融研究细致**：
    *   **核心组件消融**：逐一移除了框架中的关键设计（对称损失、`LPE`、多尺度对比、上下文预测），清晰验证了各组件的贡献。
    *   **关键模块分解**：对上下文预测中的破坏函数进行了分解，评估了打乱、噪声和遮挡三种操作各自的效果。
*   **可扩展性与鲁棒性分析**：
    *   验证了不同骨干网络（`ResNet-18`到`ResNet-152`）下的性能。
    *   系统研究了关键超参数（温度系数`τ`、掩码数量`K`、损失权重`λ_ctx`等）的敏感性。
    *   分析了预训练图像分辨率和数据量对性能的影响。
*   **可解释性验证**：通过`Grad-CAM`可视化展示了模型决策确实聚焦于目标解剖结构，与设计初衷相符。

### 6. 论文的主要结论与发现

*   **性能达到最优**：`AnaUS`在6个公开超声数据集的所有基准测试中，一致地、显著地超越了所有当前最先进的通用和超声专用自监督方法。
*   **解剖掩码是关键**：实验发现，将通用分割方法（`MCG`、`SG`）替换为`LP-SAM`生成的高质量解剖掩码，能大幅提升现有目标中心对比学习方法的性能。这证明了对超声预训练而言，**精确的解剖定位是比具体对比损失公式更为关键的决定性因素**。
*   **范式转变有效**：将自监督学习的对比单元从图像/随机区域转向临床驱动的解剖结构，是提升超声AI模型表征能力和临床相关性的正确且有效的思路。
*   **模块协同增效**：视图不变的解剖对比学习与解剖上下文预测两个目标相互补充、协同工作，共同促进了模型对细粒度解剖细节和全局结构一致性的理解。
*   **具备泛化潜力**：`LP-SAM`模块展现了零样本泛化能力，能够分割其训练期间未见过的解剖结构，说明其学到了结构化的、而非数据特异性的知识。

### 7. 优点

*   **深刻的领域洞察**：精准地识别出当前超声SSL方法的根本缺陷——忽视临床解剖逻辑，并提出了一个与临床思维高度一致的新范式。
*   **优雅且创新的技术方案**：
    *   **`LP-SAM`模块**：巧妙地结合了基础模型的强大能力与医疗领域的专业知识，通过低成本的一次性适配解决了领域鸿沟和无提示分割两大难题。
    *   **双策略学习范式**：对比学习和预测任务的设计均紧密围绕“解剖”这一核心，互补性强，逻辑清晰。
*   **实验论证极为严谨**：
    *   **全面、公平的对比**：实验设计周全，严格控制了数据、分割来源等变量，使得结论（特别是“解剖掩码是关键”这一发现）极具说服力。
    *   **深入、细致的消融**：对模型各组件和超参数进行了充分剖析，解读了每个设计的价值和最优配置。
*   **与应用落地紧密结合**：该方法计算效率高（`LP-SAM`一次性训练，编码器轻量快速），考虑了临床实时性需求，并使用Grad-CAM增强了模型的可解释性和可信度。

### 8. 不足与局限

*   **对`LP-SAM`的依赖性**：整个框架的性能建立在`LP-SAM`分割质量之上。虽然在现有数据上表现良好，但如果遇到与微调数据分布差异极大的全新超声设备、成像模式或罕见病变，`LP-SAM`的分割性能可能会下降，导致整个预训练流程失效。论文尽管展示了零样本能力，但其边界和鲁棒性未经极端考验。
*   **解剖结构的定义与覆盖**：`LP-SAM`学到的解剖结构来源于公开数据集的标注，其粒度是固定的。临床诊断有时需要关注更细微结构或病变内的异质性区域，该方法可能无法发现这些预定义之外的、潜在有意义的“解剖单元”。
*   **预训练数据的通用性**：预训练数据集`BF+CM`主要源自特定设备（Butterfly）和心脏（`CAMUS`），虽然在多个下游任务上表现优异，但其预训练分布本身可能已经引入了一定的偏差，其学到表征的普适性上界可能受限。
*   **破坏策略的设计**：上下文预测中的破坏函数旨在模拟超声伪影，其参数设定（`α`, `β`, `γ`）可能带有一定的经验性和数据集特异性，其泛化到所有超声场景的最优组合可能需要进一步验证。
*   **对比范围**：解剖级对比在`K`个采样的掩码内进行，虽然固定了`K=16`，但丢弃的极小解剖结构可能在某些精细任务中仍有价值，当前设计无法保证覆盖所有潜在的重要区域。

（完）
