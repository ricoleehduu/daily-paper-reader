---
title: "AURORA: Contextual Orthogonalization for Geometric Representation Learning in Healthcare Foundation Models"
title_zh: AURORA：医疗基础模型中几何表示学习的上下文正交化
authors: "Yuanyun Zhang, Shi Li"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.17765v1"
tags: ["query:mit"]
score: 9.0
evidence: 通过上下文正交化实现医疗健康基础模型的表示学习
tldr: 当前医疗基础模型虽预测能力强，但其潜在表征往往混杂生理严重性、干预强度、监测密度与机构流程等上下文因素，导致语义混乱且在跨机构分布偏移下不稳定。AURORA框架创新性地将表征空间正交分解为对应不同上下文因素的语义子空间，通过关系一致性学习确保各子空间几何可解释。在多项临床预测和患者检索任务上，AURORA全面超越重建、对比及自蒸馏方法，显著提升上下文解耦程度、邻域纯度与跨机构鲁棒性。该工作表明，显式构建上下文感知的潜在几何结构是医疗基础模型设计的关键维度，为增强语义清晰度和迁移稳定性提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1421, \"height\": 780, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.17765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1407, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1090, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1166, \"height\": 283, \"label\": \"Table\"}]"
motivation: 医疗基础模型将生理严重性、干预强度等上下文因素混杂在单一嵌入中，导致表征语义不清且对分布偏移脆弱。
method: AURORA通过上下文正交化将表征分解为多个语义子空间，并在各子空间内学习关系一致性目标。
result: AURORA在临床预测与检索任务中性能优于对比、重建与自蒸馏基线，并显著提升上下文解耦、邻域纯度及跨机构鲁棒性。
conclusion: 显式构建上下文语义几何结构为医疗基础模型提供了超越预测压缩的新维度，可增强解释性和鲁棒性。
---

## 摘要
近期的医疗基础模型通过大规模自监督学习取得了强大的预测性能，但其潜在表示常常将生理严重程度、干预强度、观察结构和机构工作流程纠缠到共享的嵌入方向中。尽管对下游预测有效，这些表示在语义上仍然不透明，且在上下文变化下不稳定。我们提出AURORA，即通过正交化关系对齐的自适应不确定性感知表示，一个基于上下文潜在几何的新型医疗表示学习框架。AURORA不是优化单一的统一嵌入流形，而是将表示分解为对应于不同上下文因素的正交语义子空间，并在每个子空间内学习关系一致性目标。这产生了语义解耦且几何可解释的潜在空间。在多个临床预测和检索任务中，AURORA一致优于重建、对比和自蒸馏基线，同时显著改善了上下文解耦、邻域纯度和在机构分布偏移下的鲁棒性。我们的结果表明，潜在几何本身构成了医疗基础模型设计的重要轴心，根据上下文语义显式地构建表示空间提供了超越传统预测压缩目标的互补方向。

## Abstract
Recent healthcare foundation models have achieved strong predictive performance through large scale self supervised learning, yet their latent representations frequently entangle physiologic severity, intervention intensity, observational structure, and institutional workflow into shared embedding directions. While effective for downstream prediction, such representations remain semantically opaque and unstable under contextual shift. We introduce AURORA, Adaptive Uncertainty aware Representations through Orthogonalized Relational Alignment, a new framework for healthcare representation learning based on contextual latent geometry. Rather than optimizing a single unified embedding manifold, AURORA decomposes representations into orthogonal semantic subspaces corresponding to distinct contextual factors and learns relational consistency objectives within each subspace. This induces latent spaces that are both semantically disentangled and geometrically interpretable. Across multiple clinical prediction and retrieval tasks, AURORA consistently outperforms reconstruction, contrastive, and self distillation baselines while substantially improving contextual disentanglement, neighborhood purity, and robustness under institutional distribution shift. Our results suggest that latent geometry itself constitutes an important axis of healthcare foundation model design and that explicitly structuring representation space according to contextual semantics provides a complementary direction beyond conventional predictive compression objectives.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
当前医疗基础模型主要通过大规模自监督学习优化预测性能，但其潜在表示存在一个根本性问题：
- **语义纠缠**：单一嵌入向量同时编码了生理严重程度、干预强度、观察密度、机构工作流程等多种上下文因素。
- **可解释性差与不稳定**：这种纠缠导致表示在临床上语义不透明，且在跨机构、跨上下文的分布偏移下表现不稳定。

本研究的核心含义是：**潜在表示学习不应仅仅是“预测压缩”问题，而应是一个“上下文语义几何”问题**。模型需要显式地将不同上下文因素解耦到正交的几何子空间中，从而构建更可解释、更鲁棒的医疗基础模型。

### 2. 论文提出的方法论
论文提出 AURORA 框架，核心思路是将潜在表示分解为正交的上下文语义子空间，并学习内部的关系一致性。

- **核心思想**：**上下文正交化**。将患者表示 `z` 分解为：
  `z = z(phys) + z(int) + z(obs) + z(ctx)`
  分别对应生理状态、干预结构、观察行为、残差上下文。
- **关键技术细节**：
    - **语义分解**：优于使用标注监督分解，AURORA 从 EHR 数据本身的结构中构建关系来监督每个子空间的学习。
    - **关系对齐目标**：为每个子空间 `k` 定义上下文相似关系 `Rk`。通过优化 `L(k)_align`，强制在特定上下文 (如用药模式) 上相似的患者，在相应子空间表示更接近。
    - **正交化正则**：引入 `L_orth` 惩罚不同子空间之间的交互。该机制强制生理状态、干预强度等占据几何上可分的方向，避免重新纠缠。
    - **总体优化目标**：`L_AURORA = Σ L(k)_align + λ L_orth`。框架是完全自监督的，适用于 Transformer 等主流架构。

### 3. 实验设计
- **数据集与场景**：使用一个大规模 EHR 语料库，包含诊断、用药、程序、实验室测量和生理观察等数据。
- **评估基准**：涵盖四大临床任务：
    1.  死亡率预测、脓毒症检测、再入院预测等预测任务。
    2.  患者检索任务。
    3.  上下文解耦质量评估。
    4.  在跨机构的上下文分布偏移下的鲁棒性测试。
- **对比方法**：比较了当前主流的三类自监督学习方法：
    - **重建式**：掩码自动编码器 (MAE)。
    - **对比式**：对比自监督学习。
    - **自蒸馏式**：DINO 风格的自蒸馏方法。
- **公平性**：所有方法均使用相同的 Transformer 编码器主干和参数量，下游评估均使用冻结表示+线性探针的方式，以隔离表示学习目标本身的效果。

### 4. 资源与算力
- 论文中**未明确说明**所使用的 GPU 型号、数量、训练时长及详细的算力开销。

### 5. 实验数量与充分性
实验设计较为**全面且充分**，从多个维度验证了 AURORA 的有效性：
- **预测性能**：通过 4 个下游任务 (死亡率、脓毒症、再入院、检索)，在公平设置下对比 3 类基线，验证了预测能力的提升。
- **语义解耦**：通过 3 个指标 (互信息重叠、正交性分数、上下文检索) 量化解耦质量，显著优于基线。
- **鲁棒性**：在跨机构的上下文分布偏移场景下，测试了性能衰减，证明了更强的鲁棒性。
- **几何分析**：通过邻域纯度和上下文熵分析，证明了 AURORA 产生更干净、更有组织性的潜在空间。
- 这些实验从“预测性能”、“语义结构”、“分布外鲁棒性”三个层次客观地评估了方法，逻辑完整。

### 6. 论文的主要结论与发现
- AURORA 通过显式构建**上下文正交化的潜在几何**，能够大幅改善医疗基础模型的**语义解耦能力**。
- 解耦后的表示不仅**预测鲁棒性更强、检索质量更高**，而且**对上下文分布偏移的抵抗力更强**。
- 现有自监督方法失效的一个核心原因在于其潜在表示**纠缠了生理状态、干预密度和观察上下文**，AURORA 通过几何分离解决了此问题。
- 结论指出：**潜在几何**本身应被视为模型设计的一个关键方向，为超越纯粹的预测压缩提供了新路径。

### 7. 优点
- **方法论创新**：将从 NLP/CV 中常见的“压缩/对齐”视角，提升到“上下文几何”的层面，为医疗表示学习提供了全新范式。
- **明确的问题导向**：精准指出了现有模型“语义纠缠”和“跨机构不稳定”这两大核心挑战，并提出针对性解决方案。
- **全面的评估体系**：实验不仅衡量预测的“准不准”，还深入探究潜在空间的“好不好”（解耦性、纯度、鲁棒性），评估维度新颖且关键。
- **完全的自我监督**：无需任何人工语义标注，直接从 EHR 数据模式中学习解耦，具有很强的实际应用潜力和可扩展性。

### 8. 不足与局限
- **上下文因素的定义与对齐**：自监督发现的语义子空间可能无法完美对应临床上严格定义的“标准概念”，其临床可解释性仍存在验证空间。
- **内在关联性的约束**：某些临床变量在现实中本身就强相关，强制正交化可能会导致部分信息的丢失或结构的扭曲。
- **模态覆盖有限**：当前实验主要聚焦于**结构化 EHR 数据**，框架在多模态 (如影像、文本报告、基因组学) 医疗基础模型上的有效性仍有待验证。
- **算力未披露**：缺乏对资源消耗的讨论，难以评估其在超大规模数据和模型上的训练可行性和成本。

（完）
