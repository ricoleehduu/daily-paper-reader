---
title: "AURORA: Contextual Orthogonalization for Geometric Representation Learning in Healthcare Foundation Models"
title_zh: AURORA：面向医疗健康基础模型几何表征学习的上下文正交化
authors: "Yuanyun Zhang, Shi Li"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.17765v1"
tags: ["query:mit"]
score: 9.0
evidence: 几何表示学习用于解耦医疗基础模型嵌入
tldr: 医疗基础模型表示常将生理严重程度、干预强度等上下文因素纠缠，导致语义不透明且分布不稳定。AURORA框架通过上下文正交化将表示分解为对应不同因素的正交子空间，并学习关系一致性，实现解耦与几何可解释性。在临床预测与检索任务上，AURORA显著优于重建、对比和自蒸馏基线，大幅提升上下文解耦、邻域纯度和机构分布偏移鲁棒性。该工作揭示了潜在几何设计对医疗模型的重要性，为超越预测压缩目标提供了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1421, \"height\": 780, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.17765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1407, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1090, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17765v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1166, \"height\": 283, \"label\": \"Table\"}]"
motivation: 医疗基础模型潜在表示常纠缠生理严重度、干预强度等上下文因素，导致语义不透明且上下文变化下不稳定。
method: 提出AURORA，基于上下文潜在几何将表示正交分解为对应上下文因素的语义子空间，并在各子空间内学习关系一致性目标。
result: AURORA在临床预测和检索任务中超越多种基线，显著改善上下文解耦、邻域纯度和机构分布偏移下的鲁棒性。
conclusion: 潜在几何是医疗基础模型设计的关键维度，显式按上下文语义结构化表示空间可超越传统预测压缩目标。
---

## 摘要
近期的医疗健康基础模型通过大规模自监督学习实现了强大的预测性能，但其潜在表征常常将生理严重程度、干预强度、观测结构和机构工作流程纠缠到共享的嵌入方向中。虽然对下游预测有效，但此类表征在语义上不透明，且在上下文变化下不稳定。我们提出AURORA（通过正交化关系对齐的自适应不确定性感知表征），这是一种基于上下文潜在几何的医疗健康表征学习新框架。AURORA并非优化单一的嵌入流形，而是将表征分解为对应于不同上下文因素的正交语义子空间，并在每个子空间内学习关系一致性目标。这导致了语义解耦且几何可解释的潜在空间。在多个临床预测和检索任务上，AURORA持续优于重构、对比和自蒸馏基线，同时显著改善了上下文解耦、邻域纯度和机构分布变化下的鲁棒性。我们的结果表明，潜在几何本身构成了医疗健康基础模型设计的一个重要维度，并且根据上下文语义显式地构建表征空间，提供了超越传统预测压缩目标的补充方向。

## Abstract
Recent healthcare foundation models have achieved strong predictive performance through large scale self supervised learning, yet their latent representations frequently entangle physiologic severity, intervention intensity, observational structure, and institutional workflow into shared embedding directions. While effective for downstream prediction, such representations remain semantically opaque and unstable under contextual shift. We introduce AURORA, Adaptive Uncertainty aware Representations through Orthogonalized Relational Alignment, a new framework for healthcare representation learning based on contextual latent geometry. Rather than optimizing a single unified embedding manifold, AURORA decomposes representations into orthogonal semantic subspaces corresponding to distinct contextual factors and learns relational consistency objectives within each subspace. This induces latent spaces that are both semantically disentangled and geometrically interpretable. Across multiple clinical prediction and retrieval tasks, AURORA consistently outperforms reconstruction, contrastive, and self distillation baselines while substantially improving contextual disentanglement, neighborhood purity, and robustness under institutional distribution shift. Our results suggest that latent geometry itself constitutes an important axis of healthcare foundation model design and that explicitly structuring representation space according to contextual semantics provides a complementary direction beyond conventional predictive compression objectives.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **核心问题**：现有医疗健康基础模型的潜在表示常常将生理严重程度、干预强度、观测结构、机构工作流程等异质上下文因素纠缠在同一个嵌入方向中。尽管这些表示在下游预测任务上表现良好，但其语义不透明，且在上下文分布发生变化时表现不稳定。
- **整体含义**：作者指出，医疗表征学习不应仅追求信息压缩与预测精度，更应关注**上下文语义的可解释性与几何结构**。该工作将潜在几何本身作为优化对象，提出一种新的范式：通过上下文正交化实现语义解耦，使编码空间按照临床上下文关系组织，而不仅仅是预测性压缩。

## 2. 论文提出的方法论

- **核心思想**：将病人表示分解为多个正交的语义子空间，分别对应不同的上下文因素（如生理状态、干预结构、观测行为、残余上下文），并在各子空间内施加关系对齐约束，同时用正交惩罚迫使不同子空间几何分离。
- **关键技术细节与算法流程**：
  - **上下文表示分解**：对每个样本 $x$，学习一个共享编码器 $h_\theta$ 得到 $z = h_\theta(x) \in \mathbb{R}^d$，然后将 $z$ 显式分解为：
    \[
    z = z^{\text{(phys)}} + z^{\text{(int)}} + z^{\text{(obs)}} + z^{\text{(ctx)}}
    \]
  - **关系对齐损失**：对于每个语义因子 $k$，定义一个上下文相似性算子 $R_k(x_i, x_j)$，衡量两个样本在该因子下的关联程度。在对应的子空间 $z^{(k)}$ 中，优化：
    \[
    \mathcal{L}^{(k)}_{\text{align}} = \mathbb{E}_{i,j} \left[ R_k(x_i, x_j) \, \big\| z^{(k)}_i - z^{(k)}_j \big\|_2^2 \right]
    \]
    该目标鼓励语义相近的病人在相关子空间内邻近。
  - **正交性正则化**：为防止不同子空间崩溃重叠，引入正交惩罚：
    \[
    \mathcal{L}_{\text{orth}} = \sum_{k \neq l} \big\| (z^{(k)})^\top z^{(l)} \big\|_F^2
    \]
  - **总体目标**：$ \mathcal{L}_{\text{AURORA}} = \sum_k \mathcal{L}^{(k)}_{\text{align}} + \lambda \mathcal{L}_{\text{orth}} $，其中 $\lambda > 0$ 控制语义对齐与正交解耦的权衡。
- **关键特点**：方法完全自监督，所需的语义关系结构直接从 EHR 数据中自动提取，无需额外标注，且兼容 Transformer 编码器和大规模预训练系统。

## 3. 实验设计

- **数据集/场景**：基于大规模结构化 EHR 语料，包含诊断、用药、手术、实验室测量和生理观测等时序数据。具体数据集名称未明确给出，但强调所有方法在相同语料上进行预训练。
- **Benchmark 与评估任务**：
  - 下游预测任务：死亡率预测（Mortality）、脓毒症检测（Sepsis）、再入院预测（Readmission）。
  - 检索任务：基于表示的患者检索，使用 Recall@10 评估。
- **对比方法**：
  - 掩码自编码重构（MAE）
  - 对比自监督学习（Contrastive SSL）
  - 基于自蒸馏的 DINO 风格对齐（DINO-style Self‑Distillation）
- **公平性**：所有方法使用相同的 Transformer 编码器骨干和参数预算，下游评估均通过冻结表征 + 轻量线性分类头进行，确保对比公平。

## 4. 资源与算力

- 文中**未明确提及**所使用的 GPU 型号、数量、预训练时间或总计算开销。仅说明所有方法在相同硬件和参数预算下比较，但缺少具体的算力资源描述。

## 5. 实验数量与充分性

- **实验组次**：
  - 四个下游任务（死亡率、脓毒症、再入院、检索）性能对比（表格1）。
  - 上下文解耦评估（互信息重叠、正交性得分、上下文检索一致性，表格2）。
  - 机构分布偏移下的鲁棒性对比（域内 vs 偏移 AUROC，表格3）。
  - 潜在几何分析（邻域纯度、上下文熵，表格4）。
  - 定性可视化分析（UMAP 流形对比）。
- **充分性评价**：实验设计覆盖了预测性能、语义分离度、分布鲁棒性、几何结构等多个维度，消融分析虽未单列但通过多角度逐步验证了上下文正交化的贡献。整体上，实验设置客观、对比公平，对方法优势的论证较为充分。

## 6. 论文的主要结论与发现

- AURORA 在所有下游临床预测和检索任务上均优于重构、对比和自蒸馏基线，尤其在依赖邻域组织的检索任务中提升最大。
- 上下文正交化显著降低了不同语义因子间的互信息重叠，提高了子空间正交性和上下文检索一致性，说明表示语义成功解耦。
- 在机构流程、检测频率等上下文分布发生明显变化时，AURORA 表现出更强的鲁棒性，表明将上下文因素显式分离可避免虚假关联。
- 潜在几何分析显示 AURORA 的表示具有更高的邻域纯度和更低的上下文混合熵，即局部流形区域按临床语义组织，预测性能与表示几何结构改善并行不悖。
- 核心主张：潜在几何本身应作为医疗基础模型设计的独立维度，显式结构化表示空间可超越传统预测压缩目标，带来额外的可解释性与鲁棒性。

## 7. 优点

- **问题定位精准**：准确识别了医疗表征中上下文纠缠这一现实痛点，提出将表示几何作为优化对象，视角新颖。
- **方法设计合理**：自监督的语义子空间分解、关系对齐与正交正则三者结合，既无需额外标注，又实现了可解释的解耦。
- **实验全面扎实**：从预测性能、解耦指标、分布鲁棒性、流形分析等多角度评估，对比公平且有层次。
- **理论-实证匹配**：强结果对齐了“表示几何可优化”的假设，表明组织良好的潜在空间可同时提升准确性和鲁棒性。
- **兼容性强**：框架与主流 transformer 编码器、多模态架构等均可组合，具有较好的推广潜力。

## 8. 不足与局限

- **语义因子依赖数据驱动**：子空间语义由自监督信号自动发现，可能与临床专家定义的规范概念不完全一致，部分临床变量本身高度相关，完全解耦困难。
- **数据集信息不透明**：未公开具体的数据集规模、来源及预处理细节，影响实验结果的可复现性和普适性判断。
- **算力与效率未报告**：缺少关于 GPU 使用、训练时长、内存消耗等资源细节，难以评估方法在大规模部署时的实际成本。
- **实验局限于结构化 EHR**：目前实验集中在结构化电子健康记录，对影像、文本、信号等多模态基础模型的扩展尚待验证。
- **超参数与实现敏感性**：正交损失权重 λ 等关键参数的选取与敏感性未深入讨论，不同任务下如何调优缺少指导。
- **公平性对比有限**：虽使用同一编码器，但未对比其他领域已有的解耦表示学习方法（如 β-VAE 等），方法横向对比面不够宽。

（完）
