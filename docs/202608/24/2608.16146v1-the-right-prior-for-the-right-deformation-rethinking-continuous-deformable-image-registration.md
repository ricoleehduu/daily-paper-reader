---
title: "The Right Prior for the Right Deformation: Rethinking Continuous Deformable Image Registration"
title_zh: 正确的先验对应正确的变形：对连续可变形图像配准的再思考
authors: "Hengjie Liu, Chushu Shen, Dan Ruan, Ke Sheng"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16146v1"
tags: ["query:mit"]
score: 9.0
evidence: 连续可变形图像配准方法通过隐式先验比较
tldr: 可变形图像配准模型通过参数化和优化隐式编码变形先验。本文对连续配准方法进行验证研究，比较经典B样条变换与基于INR的多种方法，包括直接建模密集位移场的INR-Dense、用INR预测B样条控制点的INR-BSCP等。研究揭示不同先验对配准性能的影响，为任务驱动的配准方法选择提供指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16146v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1225, \"height\": 974, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16146v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 999, \"height\": 723, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16146v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1232, \"height\": 608, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16146v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1247, \"height\": 258, \"label\": \"Table\"}]"
motivation: 连续可变形图像配准方法通过隐式先验比较。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
可变形图像配准模型通过其参数化和优化隐式地编码形变先验。在这项工作中，我们对连续配准方法进行了验证研究，以考察这些隐式先验如何影响不同配准任务中的性能。经典B样条变换通过其控制点结构施加局部性、平滑性和尺度约束，而近期基于INR（隐式神经表示）的方法则通过神经参数化和优化施加不同的先验。我们比较了INR-Dense（IDIR），使用基于SIREN的INR直接建模稠密位移场；INR-BSCP（SINR），用INR预测B样条控制点；D-BSCP，直接优化单尺度B样条控制点；以及MR-D-BSCP，增加了多分辨率由粗到精的方案。在个体间脑部MR配准（OASIS）和个体内呼气到吸气肺部CT配准（DIR-LAB 4DCT）上的实验揭示了在不同形变情形下的不同表现。在OASIS上，形变中等但局部复杂，D-BSCP与INR-BSCP相当或略优，表明B样条参数化在很大程度上解释了INR-BSCP的有效性。在DIR-LAB 4DCT上，呼吸运动较大且更连贯，单尺度B样条方法（D-BSCP和INR-BSCP）不太适合，而INR-Dense和MR-D-BSCP更有效。在这两项任务中，MR-D-BSCP在测试的连续参数化方法中取得了最佳性能。这些发现强调，配准精度在很大程度上取决于使所诱导的形变先验与目标运动模式相匹配，并支持将先验-形变匹配作为医学图像配准的一项实用设计原则。我们的代码将在 https://github.com/HengjieLiu/RightPriorDIR 提供。

## Abstract
Deformable image registration models implicitly encode deformation priors through their parametrization and optimization. In this work, we conduct a validation study on continuous registration methods to examine how these implicit priors affect performance across different registration tasks. Classic B-Spline transformations impose locality, smoothness, and scale through their control-point structure, whereas recent INR-based methods impose different priors through neural parameterization and optimization. We compare INR-Dense (IDIR), which directly models a dense displacement field using a SIREN-based INR; INR-BSCP (SINR), which predicts B-Spline control points with an INR; D-BSCP, which directly optimizes single-scale B-Spline control points; and MR-D-BSCP, which adds a multiresolution coarse-to-fine scheme. Experiments on inter-subject brain MR registration (OASIS) and intra-subject exhale-to-inhale lung CT registration (DIR-LAB 4DCT) reveal different behavior across deformation regimes. On OASIS, where deformations are moderate but locally complex, D-BSCP matches or slightly outperforms INR-BSCP, suggesting that the B-Spline parameterization accounts for much of INR-BSCP's effectiveness. On DIR-LAB 4DCT, where respiratory motion is larger and more coherent, single-scale B-Spline methods (D-BSCP and INR-BSCP) are less suitable, while INR-Dense and MR-D-BSCP are more effective. Across both tasks, MR-D-BSCP achieves the best performance among the tested continuous parameterizations. These findings highlight that registration accuracy depends strongly on matching the induced deformation prior to the target motion pattern, and support prior-deformation matching as a practical design principle for medical image registration. Our code will be available at https://github.com/HengjieLiu/RightPriorDIR.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机与背景**  
  - 可变形图像配准（DIR）需要估计非线性空间变换，以对齐不同图像中的解剖结构。  
  - 配准模型通过参数化和优化过程**隐式编码形变先验**，例如经典 B 样条自由形变（FFD）通过控制点结构引入局部性、平滑性和尺度约束；基于隐式神经表示（INR）的方法则通过神经网络参数化引入另一类先验。  
  - 此前两项代表性工作给出了看似矛盾的结果：  
    - **IDIR（本文称 INR-Dense）**：用 SIREN MLP 直接预测稠密位移场，在呼气到吸气的肺 CT 配准中表现良好。  
    - **SINR（本文称 INR-BSCP）**：认为直接预测稠密位移对脑 MR 配准不理想，改用 INR 预测 B 样条控制点值。  
  - 这引出一个核心问题：**连续配准方法的性能提升，究竟来自 INR 本身、B 样条参数化，还是模型先验与目标形变模式之间的匹配？**

- **整体含义**  
  - 本文是一项**验证研究**，旨在从“先验匹配”视角系统比较连续 DIR 参数化方法。  
  - 核心主张：**配准质量关键取决于模型诱导的形变先验是否匹配目标运动模式**，包括位移幅度、平滑性、局部性、方向性、频率内容和优化路径。  
  - 这为医学图像配准提供了一条实用设计原则：**先验-形变匹配**，而不仅仅是追求更新颖的连续表示或网络结构。

## 2. 论文提出的方法论

- **DIR 问题形式化**  
  - 目标是估计位移场 \(u(x)\)，使移动图像 \(M\) 对齐固定图像 \(F\)：  
    \[
    \hat{u} = \arg\min_{u} \left[ \mathcal{L}_{\text{sim}}(M \circ \phi, F) + \lambda R(u) \right],
    \quad \phi(x) = x + u(x)
    \]  
  - \(\mathcal{L}_{\text{sim}}\) 为图像相似性损失，\(R(u)\) 为正则项，\(\lambda\) 控制相似性与规则性之间的权衡。  
  - 论文采用 alignment–regularity characteristic（ARC）框架，在多个正则化强度下比较完整权衡谱，而不是单一工作点。

- **比较的连续配准模型**  
  - **B 样条 FFD（D-BSCP）**  
    - 位移由控制点位移的立方 B 样条张量积插值得到，控制点间距决定形变尺度：粗网格更平滑、更全局，细网格允许更局部运动。  
  - **INR-Dense（IDIR）**  
    - 使用基于 SIREN 的坐标 MLP \(f_\theta\)，输入空间坐标 \(x\)，直接输出位移：\(u_\theta(x) = f_\theta(x)\)。  
    - 网络对输入坐标可微，便于通过自动微分计算空间导数。  
  - **INR-BSCP（SINR）**  
    - 使用相同的 SIREN MLP，但预测 B 样条控制点位移 \(c_{p,q,r} = f_\theta(x^{cp}_{p,q,r})\)，再由 B 样条插值得到稠密位移场。  
  - **MR-D-BSCP**  
    - 在 D-BSCP 上增加**多分辨率由粗到精方案**，例如 32-16-8-4 或 8-4-2 的控制点网格，并配合图像金字塔。  
    - dyadic \(2\times2\times2\) 网格嵌套特性允许通过解析节点插入快速从粗网格上采样到细网格。  
  - **MR-INR-Dense**  
    - 改编自 Dual-INR，用于肺 CT 配准，将运动分解为粗、细两个 INR 分支，实现多尺度 INR 表示；本文去除其肺叶分割损失（影响可忽略）。

- **目标场拟合诊断**  
  - 为分离**表示容量**与**配准优化行为**，将每种参数化用监督 MSE 损失拟合一个强外部参考位移场：OASIS 使用 SITReg 场，DIR-LAB 使用 pTVReg 场。  
  - 这些参考场不是 ground truth，而是高表现的可信形变，用于检验模型在给定目标时能否表示该形变。

## 3. 实验设计

- **数据集与场景**  
  - **OASIS 脑 MR 配准（个体间）**  
    - 414 例 1 mm 各向同性 T1 加权 MR；按 314/20/80 划分训练/验证/测试，另随机采样 100 对测试对进行评估。  
    - 形变特征：中等幅度、局部复杂。  
  - **DIR-LAB 4DCT 肺 CT 配准（个体内，呼气到吸气）**  
    - 全部 10 个 4DCT 病例，使用两个极端呼吸相位 T00 和 T50。  
    - 遵循 pTVReg 约定：将扫描重采样到 1 mm 各向同性进行配准，再映射回原始网格计算标记点 TRE。  
    - 形变特征：幅度较大、方向更一致、更平滑的呼吸运动。  
    - 由于样本只有 10 对，未评估学习式基线。

- **对比方法**  
  - 核心连续方法：INR-Dense、INR-BSCP、D-BSCP、MR-D-BSCP；肺任务额外对比 MR-INR-Dense。  
  - OASIS 外部基线：VoxelMorph、TransMorph、VFA、SITReg、Greedy。  
  - DIR-LAB 外部基线：pTVReg（优化式 SOTA）。

- **评估指标与协议**  
  - **OASIS**：35 个分割标签的平均 Dice 与平均 95 百分位 Hausdorff 距离（HD95）。  
  - **DIR-LAB**：每例 300 个手动标记点对的平均目标配准误差（TRE）。  
  - **规则性**：前景掩膜内对数 Jacobian 行列式的标准差 \(\text{SD}(\log J)\)。  
  - 使用 ARC 曲线在多个正则化强度下比较，外部固定基线作为单工作点加入图中。  
  - 目标场拟合在 OASIS 随机 10 对和 DIR-LAB 全部 10 对上进行，并与无监督配准对比。

## 4. 资源与算力

- **GPU**  
  - 所有实验均在 **NVIDIA RTX 6000 Ada Generation GPU**（48 GB 显存）上运行。  
  - 文中未明确说明 GPU 数量或是否使用多卡；从实验描述看应为单卡环境。

- **优化配置**  
  - 所有方法使用 Adam，总优化预算 **2500 步**；MR-D-BSCP 的步数在不同分辨率级别之间分配。  
  - INR 方法与直接 B 样条方法参数规模不同，未强制共享学习率；对两者均采用 \(\{10^{-2},10^{-3},10^{-4},10^{-5}\}\) 粗扫描。  
    - INR 方法：官方 \(10^{-4}\) 稳定，被采用；\(10^{-2}\)、\(10^{-3}\) 不稳定。  
    - 直接 B 样条方法：\(10^{-3}\) 最稳定且最终性能最佳；\(10^{-2}\) 更快但精度稍降。

- **运行时分析（OASIS，输入图像到保存稠密位移场）**  
  - 优化后的快速 schedule 在 OASIS-100 上几乎保持 Dice 精度，同时大幅缩短运行时。  
  - 例如 MR-D-BSCP：从 2500 步 cold 152.0 s / warm 62.4 s 降至 fast schedule cold 101.1 s / warm 11.0 s。  
  - 但论文**未给出所有方法在所有数据集上的完整训练总时长**，只提供了 OASIS 上的运行时对比。

## 5. 实验数量与充分性

- **实验规模概览**  
  - 两个不同形变机制的数据集：OASIS 脑 MR 和 DIR-LAB 肺 CT。  
  - 4 个核心连续参数化 + 1 个肺任务额外 MR-INR-Dense。  
  - OASIS 上 5 个外部基线，包括 4 个学习式方法和 1 个优化式方法；DIR-LAB 上与 pTVReg 对标。  
  - ARC 框架下多正则化强度的全谱比较，而非单点比较。  
  - 目标场拟合诊断：OASIS 10 对 + DIR-LAB 10 对。  
  - 代表性案例分析：DIR-LAB case01（表现相近）和 case08（大运动下单尺度方法困难）。  
  - OASIS 运行时缩短实验：比较 base 与 fast schedule 的 Dice、步数、冷/热运行时间和峰值 GPU 显存。

- **充分性与客观性评价**  
  - **较充分的方面**  
    - 通过 D-BSCP 与 INR-BSCP 的对比，有效隔离了“B 样条参数化”与“INR 预测”的贡献。  
    - ARC 全谱比较避免了在单一正则化点上的偏颇结论。  
    - 目标场拟合诊断分离了表示容量与优化路径，为解释失败模式提供了依据。  
    - 学习率采用统一粗扫描，减少了调参偏差。  
  - **局限与潜在偏差**  
    - 仅覆盖两个任务/解剖部位，尚不足以推广到更多形变模式（如滑动运动、肿瘤形变等）。  
    - DIR-LAB 只有 10 对样本，统计力度较弱；且未与学习式基线在该任务上比较。  
    - 未进行统计显著性检验，主要依赖均值与标准差/椭圆范围。  
    - 外部参考场（SITReg、pTVReg）被当作“高表现场”，但它们本身不是 ground truth，可能引入偏差。  
    - 未系统隔离不同正则项形式的影响；固定 2500 步可能对不同参数化并非完全公平，但对本研究设计是可接受的折中。

## 6. 论文的主要结论与发现

- **不同任务需要不同的形变先验**  
  - OASIS 脑 MR：变形中等但局部复杂，**D-BSCP 与 INR-BSCP 相当或略优**，说明 B 样条控制点参数化已经解释了 INR-BSCP 的大部分效果；INR 本身并未带来额外优势。  
  - DIR-LAB 肺 CT：呼吸运动大且连贯，**单尺度 B 样条方法（D-BSCP 和 INR-BSCP）不太适合**，在弱正则下可能急剧失败；INR-Dense 更鲁棒。  
  - **MR-D-BSCP 显著改善 B 样条方法**，在肺任务上接近 pTVReg：TRE 为 \(0.917 \pm 0.148\) mm，而 pTVReg 为 \(0.919 \
